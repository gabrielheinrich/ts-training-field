/**
 * A function which returns the first element of an array.
 * @param array The array of which the first value is returned.
 */
export function getFirstValue(array: any[]): any {
   return array[0];
}
